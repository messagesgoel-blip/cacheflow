# CodeRabbit Full Audit Sweep (2026-03-08)

## Purpose
Create a review-only PR that touches broad repository surfaces so CodeRabbit can run an expanded pass across existing code, not just one feature diff.

## Scope
- Directories: `api/`, `lib/`, `worker/`, `web/`, `scripts/`, `docs/`
- Change type: whitespace-only EOF touch (no intended runtime behavior changes)

## Safety
- Mark PR as **DO NOT MERGE**.
- Use findings as backlog input for follow-up fix PRs.

## Next Step
Run CodeRabbit on the audit PR and triage findings into severity buckets before implementation.
