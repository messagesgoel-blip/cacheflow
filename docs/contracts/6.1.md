# Contract: 6.1 — Dashboard Quota Alert Slices

**Sprint:** 6.1
**Agent:** ClaudeCode
**Date:** 2026-03-08

---

## Slices Implemented

### 1. `StorageHero` — Quota-alert consistency

**File:** `web/components/dashboard/StorageHero.tsx`

| Aspect | Before | After |
|--------|--------|-------|
| Warning threshold | `>= 75%` | `>= 80%` |
| Critical threshold | `>= 90%` | `>= 95%` |
| Bar width clamping | `Math.min(percent, 100)` only | `Math.max(0, Math.min(percent, 100))` everywhere |
| Free-space display | Could render negative bytes | Clamped via `Math.max(0, total - used)` |
| Severity indicators | None | "⚠️ Storage Low" at ≥80%, "🚨 Storage Critical" at ≥95% shown on aggregate bar and per-provider breakdown cards |
| Provider Breakdown keys | `provider.providerId` (non-unique for multi-account) | `${providerId}:${accountEmail \|\| displayName}` |
| Provider Breakdown colors | Always provider brand color | Overridden to yellow/red at warning/critical thresholds |

**Severity label behavior:**
- Aggregate usage bar: label appears inline next to percentage
- Provider Breakdown cards: label appears inline next to percentage
- Priority Providers mini-bars: threshold-aware color + free-space shows `Math.max(0, ...)` clamped value

---

### 2. `ProviderCapacityBar` — Accessibility + over-quota correctness

**File:** `web/components/dashboard/ProviderCapacityBar.tsx`

| Aspect | Before | After |
|--------|--------|-------|
| Thresholds | Used provider brand colors only | `>= 95%` → `var(--cf-red)`, `>= 80%` → `var(--cf-amber)`, else provider color |
| ARIA attributes | None | `role="progressbar"`, `aria-valuenow` (clamped 0–100), `aria-valuemin=0`, `aria-valuemax=100`, `aria-label` |
| Free-space display | `total - used` (could be negative) | `Math.max(0, total - used)` for "free" label |
| Over-quota | Not handled | Shows `"{X} over quota"` when `used > total` |
| Severity icons | None | 🚨 at ≥95%, ⚠️ at ≥80% with title attributes |
| Mapping key | `provider.providerId` | `${providerId}:${accountEmail \|\| displayName \|\| 'default'}` |
| Bar width | `Math.min(percent, 100)` | `Math.max(0, Math.min(percent, 100))` |

---

### 3. `Sidebar` — Storage-card threshold alignment

**File:** `web/components/Sidebar.tsx`

| Aspect | Before | After |
|--------|--------|-------|
| Warning threshold | `> 75%` | `>= 80%` |
| Critical threshold | `> 90%` | `>= 95%` |
| Aggregate bar width | `${aggregateQuota.percent}%` (unclamped) | `Math.max(0, Math.min(percent, 100))%` |
| Aggregate status text | None | "Storage Low" (amber) at ≥80%, "Storage Critical" (red) at ≥95% |
| Per-account bar width | `${usagePercent}%` (unclamped) | `Math.max(0, Math.min(usagePercent, 100))%` |
| Per-account status text | Always shows `{percent}%` | Shows "Storage Low" or "Storage Critical" when thresholds hit; percent otherwise |
| Navigation behavior | Unchanged | Unchanged |

---

## Threshold Reference (all components)

| Level | Condition | Bar color | Label |
|-------|-----------|-----------|-------|
| Normal | `< 80%` | Provider brand / blue | (none) |
| Warning | `>= 80%` | Yellow / amber | "Storage Low" / ⚠️ |
| Critical | `>= 95%` | Red | "Storage Critical" / 🚨 |

---

## Verification

| Check | Result |
|-------|--------|
| `cd web && npx tsc --noEmit` | Pass — only pre-existing `.next/types/validator.ts(182,1): error TS1128` (Next.js build artifact, not source code) |
| No new TypeScript errors | Confirmed |
| Bar widths clamped 0–100 | All `style.width` values use `Math.max(0, Math.min(…, 100))` |
| Free-space never negative | All free-space displays use `Math.max(0, total - used)` |
| Stable React keys | Multi-account safe via `${providerId}:${accountEmail \|\| displayName}` pattern |
| Existing navigation preserved | Sidebar `onNavigate` untouched |
| Auth/token behavior | Not modified |
