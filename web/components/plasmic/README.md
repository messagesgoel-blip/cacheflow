# Plasmic Generated Files

This directory is reserved for Plasmic-generated files managed by:

- `npm run plasmic:sync`
- `npm run plasmic:watch`

Rules:

- Do not store secrets here.
- Avoid hand-editing generated component files.
- Connect generated components to app state/backend through repo-owned wrapper
  components in `web/components/`.

