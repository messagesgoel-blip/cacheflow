#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$repo_root"

if command -v coderabbit-local-review >/dev/null 2>&1; then
  coderabbit-local-review start --type committed >/dev/null 2>&1 || true
fi
